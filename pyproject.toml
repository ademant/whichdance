[project]
name = "whichdance"
version = "0.1.0"
description = "Predict traditional folk dance type from a tune's audio"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
